---
const { class: className } = Astro.props;
---

<span
  class={`inline-flex items-center gap-1.5 bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300 text-sm px-3 py-1 rounded-full border border-gray-200 dark:border-gray-700 ${className ?? ""}`}
>
  <slot />
</span>
